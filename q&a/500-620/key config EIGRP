三个“匹配”：

1. **AS 号码**：接口下的命令必须对应正确的 AS 号（此题中是 100）。
2. **Mode MD5**：必须显式敲入这条命令，否则默认是文本或无认证。
3. **Key Chain 名称**：定义的名字和接口引用的名字必须一模一样。

既然你正在准备 CCNP，理清不同协议在**认证（Authentication）**上的差异非常重要。Cisco 考试经常在配置命令的层级（接口 vs 进程）以及默认行为上挖坑。

以下是常见动态路由协议认证方式的对比整理：

### 路由协议认证对比表

| **协议**   | **认证级别 (Level)**        | **认证模式**            | **关键配置特点**                                             |
| ---------- | --------------------------- | ----------------------- | ------------------------------------------------------------ |
| **EIGRP**  | **接口** (Interface)        | 仅支持 MD5 / SHA (离散) | 必须使用 `key chain`；必须手动开启 `mode md5`。              |
| **OSPFv2** | **接口** 或 **区域** (Area) | 明文 / MD5              | 可以在区域级别开启，但密钥通常在接口指定。支持不连续的 Key ID 滚动。 |
| **BGP**    | **进程/邻居** (Neighbor)    | MD5                     | 命令最简单：`neighbor [IP] password [Key]`。不需要 key chain。 |
| **IS-IS**  | **接口** 或 **进程**        | 明文 / MD5              | 分为接口认证（Hello 包）和区域/域认证（LSP/CSNP）。          |

------

### 核心差异与易错点

#### 1. EIGRP：最“死板”的配置

正如你刚才那道题看到的，EIGRP 的认证是**必须**配合 `key chain` 的。

- **坑点**：即使你配置了 `key-chain`，如果你忘了敲 `ip authentication mode eigrp 100 md5`，它还是不会生效。
- **AS 号一致性**：接口下的认证命令必须带上正确的 AS 号。

#### 2. OSPFv2：灵活但层次多

OSPF 的认证可以“全局开启，接口赋值”：

- **区域认证**：在 `router ospf` 进程下敲 `area 0 authentication message-digest`。这只是**开启**了开关，你仍需去每个接口下配置密钥。
- **接口认证**：直接在接口下敲 `ip ospf message-digest-key 1 md5 [Password]`，这会自动覆盖区域级别的设置。

#### 3. BGP：最直接

BGP 不使用 Key Chain（除非是较新的增强版配置）。

- 它直接利用 TCP 报文头的 MD5 选项。
- 只要 `password` 后面跟着的字符串对不上，TCP 三次握手都无法完成，邻居关系直接停在 `IDLE` 或 `ACTIVE` 状态。

------

### 总结建议

在做 CCNP 实验或题目时，遵循这个排查思路：

1. **有没有开启模式？**（特别是 EIGRP 的 `mode md5` 和 OSPF 的 `message-digest` 关键字）。
2. **Key ID 是否匹配？**（EIGRP 和 OSPF 的 `key 1` 必须两端对应）。
3. **调用名字是否写对？**（Key chain 的名字大小写敏感）。

